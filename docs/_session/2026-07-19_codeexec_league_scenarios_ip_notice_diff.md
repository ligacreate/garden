# DIFF-ON-REVIEW — инфоблок об интеллектуальной собственности над сеткой «Сценарии лиги»

**Дата:** 2026-07-19
**Автор:** codeexec (VS Code)
**Статус:** ♻️ ВОШЁЛ В БАТЧ → `2026-07-19_codeexec_league_frontend_batch_diff.md` (п.2). Этот файл — история; актуальный дифф там (инфоблок + per-scenario usage_note одним окном).
**Файл:** `views/BuilderView.jsx` (вкладка `league`, где рендерятся 9 is_public-сценариев)

## Что делаем
Над сеткой карточек в табе «Сценарии лиги» — спокойный инфоблок (не алерт) с уведомлением об интеллектуальной собственности. Текст — **дословно**, не резать. Показывается только когда в библиотеке есть сценарии (`leagueScenarios.length > 0`), чтобы не висеть над пустым состоянием.

## Стиль
`bg-slate-100 border border-slate-200 rounded-2xl` + иконка `Info` (slate-400) + текст `text-slate-600 leading-relaxed`. Тёплый нейтральный тон из палитры проекта — отличается и от белых карточек, и от фона страницы, читается спокойно, без алертных цветов.

## Изменения (2 точки в одном файле)

### 1. Импорт иконки — строка 3
```diff
- import { FileText, Download, Plus, X, Printer, Leaf, ArrowUp, ArrowDown, Save, FolderOpen, Trash2, Globe, Layout, GripVertical, PenLine, Upload } from 'lucide-react';
+ import { FileText, Download, Plus, X, Printer, Leaf, ArrowUp, ArrowDown, Save, FolderOpen, Trash2, Globe, Layout, GripVertical, PenLine, Upload, Info } from 'lucide-react';
```

### 2. Инфоблок над сеткой — таб `league` (сейчас строки 1145–1151)
Вставить первым ребёнком контейнера таба, ВЫШЕ строки прогресса и ВЫШЕ `<ScenarioList>`:
```diff
                ) : (
                    <div className="flex-1 overflow-y-auto animate-in fade-in duration-300">
+                       {leagueScenarios.length > 0 && (
+                           <div className="mb-5 flex items-start gap-3 bg-slate-100 border border-slate-200 rounded-2xl px-5 py-4">
+                               <Info size={18} className="text-slate-400 shrink-0 mt-0.5" />
+                               <div className="text-sm text-slate-600 leading-relaxed space-y-2">
+                                   <p>Сценарии этого раздела являются интеллектуальной собственностью ведущих Лиги и распространяются в учебных целях в качестве примера.</p>
+                                   <p>Использование данных сценариев возможно лишь частично в формате идей и отдельных практик, без полного повторения сценария, и предполагает обязательное упоминание имени ведущей.</p>
+                               </div>
+                           </div>
+                       )}
                        {leagueScenarios.length > 0 && (
                            <div className="mb-4 text-xs text-slate-500">
                                Прогресс: изучено {completedLeagueScenariosCount} из {leagueScenarios.length}
                            </div>
                        )}
                        <ScenarioList
                            scenarios={leagueScenarios}
                            variant="league"
                            onLoad={handleOpenLeagueScenario}
                            emptyMessage="В библиотеке Лиги пока пусто"
                            completedIds={completedLeagueScenarioIds}
                        />
                    </div>
                )}
```

## Текст (дословно, для сверки)
> Сценарии этого раздела являются интеллектуальной собственностью ведущих Лиги и распространяются в учебных целях в качестве примера.
> Использование данных сценариев возможно лишь частично в формате идей и отдельных практик, без полного повторения сценария, и предполагает обязательное упоминание имени ведущей.

Две логические фразы оригинала → два `<p>` в блоке `space-y-2` (перенос строки оригинала сохранён, читаемость выше). Ни слова не срезано/не добавлено.

## Судейские моменты (нужно решение с 🟢)
- **JC-1 — размещение относительно прогресса.** Предлагаю: инфоблок самым верхом, под ним строка «Прогресс: изучено N из M», под ней сетка. Альтернатива — блок между прогрессом и сеткой. Рекомендую верх (уведомление важнее счётчика).
- **JC-2 — гейт `length > 0`.** Скрываю блок при пустой библиотеке (сейчас там 9 сценариев, так что практически всегда виден). Если нужно показывать всегда — уберём гейт.

## План верификации (после 🟢 apply)
1. `npm run build` — чисто.
2. Таб «Сценарии лиги»: инфоблок над карточками, текст дословный, спокойный тон (не алерт), читаемый на десктопе и мобильном (flex не ломается).
3. Регресса верстки сетки/прогресса нет.

## Выкат
Чистый фронт → `git push` (GitHub Actions → FTP на liga, окно 403 ожидаемо). БД не трогает. Можно совместить с окном следующих фронт-правок.
