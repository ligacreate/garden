# Legacy Supabase Scripts

These scripts were used for one-time Supabase migrations and cleanup.

They are intentionally moved out of the active scripts root to avoid accidental use in the current API/Auth architecture.

If you still need historical data extraction, run them manually and treat them as archived utilities.
