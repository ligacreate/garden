# HANDOVER — Лига-подписка / Фаза 3 · состояние на 2026-07-10 (вечер)

Заменяет [`_session/252`](2026-07-10_252_handover.md). Для нового codeexec-чата. Кратко, по делу.

## ✅ Задеплоено и живо на проде
- **mode=admit** (`TG_ACCESS_MODE=admit`): TG-reconcile + join-поллер. Поллер авто-approve оплаченных
  (`chat_join_request`, матч по `telegram_user_id`/`@username`). Kick — planned, НЕ исполняется.
- **Товаро-гейт вебхука** (`isLigaProduct`, [`_session/253`](2026-07-10_253_codeexec_phase3_product_gate_deployed.md)): Лига-`paid_until` только за Лига-товар. Класс Старостиной закрыт на источнике.
- **Развязка слоёв В1** ([`_session/256`](2026-07-10_256_codeexec_phase3_v1_decouple_diff.md)+[`257`](2026-07-10_257_codeexec_phase3_v1_decouple_applied.md), коммит `54f7146`): **Лига-неоплата больше НЕ трогает `access_status`.**
  Ни reconcile, ни вебхук (finish/deactivation, BotHunter-expired) не ставят `paused_expired`, logout не шлётся.
  8 застрявших `paused_expired`→`active` возвращены (data-apply). Итог: `active 48 · paused_manual 12 · paused_expired 0`.
- **RPC `admin_payment_stats_by_month`** (phase47, коммит `e29e976`): применён на прод-БД, SECURITY DEFINER + `is_admin()`-guard (owner→forbidden ✓). Пока только фронт нужен (258/259).
- Бот `@ligagardenbot` — админ в канале (`-1002377682177`) + чате (`-1002432957741`). Токен в `.env`, НЕ ревокнут.
- BILLING_SANDBOX=0 — боевой checkout.

## 🎯 Модель «кабинет-первый» (В1, зафиксирована и живёт)
**Два независимых слоя доступа:**
- **Платформенный доступ** = `access_status='active'` (RLS `has_platform_access`) → кабинет + **курс по роли**. Больше НЕ зависит от Лига-оплаты.
- **Лига-подписка** = `subActive = paid_until >= now` → Лига-поверхности (TG, будущие кнопки). `subscription_status` (active/overdue/…) — репортинг-флаг (напоминания 1f).
- **Курс НЕ трогаем** (решение стратега): `pvlRoleResolver` как есть — курс видят applicant/intern/mentor/admin; leader/curator нет. `intern` курс сохраняет.
- Дизайн: [`_session/254`](2026-07-10_254_codeexec_phase3_cabinet_first_design.md), числа [`255`](2026-07-10_255_codeexec_phase3_cabinet_first_step1_recon.md).

## ⏳ Approved, ждёт ОКНА 403 (фронт-деплой, FTP clean-slate)
- **258 — кнопки «Вступить в канал/чат Лиги»** в «Моя подписка» ([ProfileView.jsx](../../views/ProfileView.jsx)), гейт `subActive`, standing-ссылки
  (канал `t.me/+dVRWs_cl2VA3OTVi`, чат `t.me/+GH0sjSaUzOc2N2Zi`). **v1 без membership-check** (клик уже-вступившего безвреден). Diff-only, НЕ применён.
- **259 — статистика оплат по месяцам** (AdminPanel таб «Статистика», dataService `getPaymentStatsByMonth` → RPC phase47). Diff-only, НЕ применён.
- ⚠️ **Оба — только в доках, код НЕ написан в рабочем дереве** (это diff-on-review). При старте окна 403: сначала реализовать 258+259 по их diff'ам, потом деплой.

## 📦 Git — НЕ пушить до окна 403
Куча незапушенных коммитов (бэкенд-код В1 + товаро-гейт + доки + миграции). **`push-server/**` и `database/**` НЕ в `paths-ignore`** → пуш дёрнет FTP-воркфлоу (clean-slate → окно 403). Прод уже обновлён rsync'ом/owner-apply — git-push для бэкенда не нужен. Всё уедет ОДНИМ пушем в окно 403 вместе с фронтом (258/259).

## 🧾 Легаси-хвост Лига-без-профиля (из [`255`](2026-07-10_255_codeexec_phase3_cabinet_first_step1_recon.md))
- **hinesta@mail.ru — ЗАКРЫТ:** покрыта ручной mark-paid (`paid_until=2026-08-01`); застрявший вебхук-лог 218 помечен `SUPERSEDED_BY_MANUAL_MARK_PAID`, без повторного начисления.
- **Осталось 2 сироты:** `evantipina@ya.ru`, `ksu.shik@mail.ru` (Лига-платежи 2000₽, профиля нет). Нужно: завести профиль/пригласить зарегистрироваться → поллер сматчит по email/username. Отдельный шаг, НЕ запущен.
- Остальные profile-not-found (13 логов) — 9 не-Лига (товаро-гейт скипает) + 4 на этих 2 сирот + 1 finish.

## 🔭 Открытые треки (не начаты)
1. **SMTP-доставка join-ссылок** после `plan_payment` (гейт `plan_code∈Лига`) — §10-дефолт стратега (для off-platform/email).
2. **Prodamus «Платный контент»** = обе join-ссылки на **каталожном** Лига-товаре (кабинет Оли; наш checkout строит товар ad-hoc → к нему paid_content не привязан).
3. **Привязка Telegram к профилю** — чтобы поллер авто-одобрял кликнувших кнопку (иначе pending → ручное одобрение).
4. **Membership-check** (getChatMember) — скрывать «Вступить» уже-вступившим. Опц., ~полдня, низкий приоритет ([258](2026-07-10_258_codeexec_phase3_join_buttons_diff.md) §оценка).
5. **KICK Шиловой** (gated, planned) + **cutover TargetHunter** — при переходе в `mode=live`.
6. **Напоминания 1f** (T-7/3/1/expired) — использует новый `subscription_status='overdue'`.

## Инфра
- push-server: `/opt/push-server/`, systemd `push-server.service`, rsync `--exclude=.env --exclude='*.test.mjs'` + restart, smoke `/health`.
- Прод-БД: owner (`gen_user`) через `DATABASE_URL` (только с прод-VM). Правила: diff-on-review, dry→commit, self-guard.
- Тесты push-server: `node --test billingLogic.test.mjs` (18/18 на 2026-07-10, В1).
- Эндпоинты TG-доступа (requireAdmin): `POST /api/tg-access/run?mode=`, `GET /planned`, `POST /confirm-kicks`.

## Ключевые доки
253 товаро-гейт · 254 дизайн В1 · 255 recon-числа · 256 diff В1 · 257 apply В1 · 258 кнопки (diff) · 259 статистика (diff).
