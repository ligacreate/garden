# DESIGN-001 типо-T2 батч 3 (админ/менторские) — отчёт

**Дата:** 2026-06-19. **Автор:** codeexec. **Статус:** ✅ собран зелёным, закоммичен, задеплоен. Правила батча 1.
**Файлы:** MentorDashboardView, AdminPvlProgress.

## MentorDashboardView (ПВЛ-стиль — как батч 1)
Заголовки (цвет сохранён):
- «ЛК ментора» (страничный, text-3xl `#4A3728`) → `.h-display`.
- «Мои менти» / «Очередь проверок» / «Риски по дедлайнам» (text-2xl `#4A3728`) → `.h-section`.
- Числа сводки (text-4xl `#C8855A`) — не заголовки, **не тронуты**.

Eyebrow'ы (→ `.text-meta` + сохранён `text-ink-mute`, как батч 1):
- Stat-лейблы «Всего менти / Активных проверок / Менти в риске / Просроченных ДЗ» (×4).
- `<th>` шапки таблиц (×2).

Пилы: `Pill` status-badge → de-caps, цвет (tone) сохранён.

## AdminPvlProgress (минимально)
- «Прогресс студентов ПВЛ» (панель-заголовок, font-display text-lg) → `.h-section` slate-900.
- «Сертификационные завтраки» (под-блок, Bricolage без размера) → **`.h-sub`** (Onest 16, под-уровень) slate-900. ← первое применение `.h-sub`.
- Капслока нет (0 uppercase) — только заголовки.

## Шкала-сводка
1 `.h-display`, 4 `.h-section`, 1 `.h-sub`. Все с сохранёнными цветами (`#4A3728` / slate-900). Eyebrow'ы → `.text-meta` (color preserved). Пилы de-caps.

## Не тронуто
- Цвета/палитра/хексы — НЕ тронуты (kept color-классы).
- Числа сводки/прогресса — не заголовки.
- `.section-title`/`.section-kicker` — не удалял.

## Verified
Сборка зелёная; 0 капслока в обоих файлах; цвета сохранены; диф 13+/13−.

## Замечание (консистентность eyebrow'ов между батчами)
- Батч 1 + батч 3 (ПВЛ-кластер): ink-mute eyebrow'ы переведены в `.text-meta text-ink-mute` (de-caps + meta-scale 13px, цвет сохранён).
- Батч 2 (member-facing): eyebrow'ы de-capsed **в месте** (без `.text-meta`) — из-за form-`<label>`'ов и white-on-dark лейблов (см. _203 §развилка).
- Итог по обоим: ноль декоративного капслока, цвета строго не тронуты. Если нужна полная унификация member-facing kicker'ов в `.text-meta` — отдельный микрошаг по запросу стратега.
