# «Сценарии лиги» — новый текст инфоблока + компактная заметка «От автора» — apply

**Дата:** 2026-07-20
**Автор:** codeexec (VS Code)
**Статус:** ✅ ПРИМЕНЕНО (🟢, две правки в `views/BuilderView.jsx`)

## 1. Текст общего инфоблока
Второй абзац заменён (первый без изменений):
> Проводить встречу по чужому сценарию целиком нельзя — если автор прямо не разрешил это в заметке к сценарию. Использование возможно частично, в формате идей и отдельных практик, и предполагает обязательное упоминание имени ведущей.

## 2. Заметка «От автора» на карточке — схлопнута в одну строку
Был многострочный абзац под названием → **растягивал карточку, ломал ровность сетки**.
Стало: компактная плашка (иконка + **ОТ АВТОРА** + текст `line-clamp-1`, кегль 10px) **на строке статуса**, рядом с бейджем «Не изучено».

**Почему на строке статуса, а не отдельным блоком под названием:** только так высота карточки НЕ растёт. Отдельная строка-плашка добавляла ~32px → строка с заметкой оказывалась выше остальных. Разместив плашку на уже существующей строке бейджа, добились нулевого прироста.

Полный текст заметки в просмотре (`DocumentPreviewModal`, ~стр. 367) — **не трогали**, там он целиком.

## Verify (Playwright, LOCAL_DB, 9 сценариев, заметка только у id 26)
| Проверка | Результат |
|---|---|
| Высота карточки id 26 (с заметкой) | **222px** |
| Соседняя в ряду (без заметки) | 222px — Δ0 |
| Карточка в другом ряду (ряд 2 / ряд 3) | 222px / 222px — Δ0 |
| Новый текст инфоблока в DOM | ✅ есть |
| Старый текст («без полного повторения сценария») | ✅ убран |
| Заметка в одну строку, обрезается по ширине | ✅ |

**Итог:** все карточки идентичной высоты, сетка ровная; заметка видна одной строкой на карточке и целиком — в просмотре. `npm run build` чисто.
