# Recon FEAT-016 + FEAT-017 — code surface only

**Адресат:** VS Code Claude Code (executor).
**Автор:** стратег (claude.ai).
**Создано:** 2026-05-07.

DB-side recon (схемы pvl_*-таблиц, RLS-policies, sample data, counts)
делает стратег самостоятельно через ssh+psql — **НЕ дублировать.**

Этот промпт — **только code surface.** Read-only. НЕ apply, НЕ commit,
НЕ предлагать план миграции.

## Что нужно

### 1. PVL-логика в коде Garden

- `views/PvlPrototypeApp.jsx` — учительская:
  - Структура файла (сколько строк, какие внутренние компоненты).
  - Какие view-секции внутри (Дашборд / Мои менти / Очередь проверок /
    Трекер / Календарь / Библиотека / Глоссарий / Чат с менти /
    Результаты / Сертификация / Настройки) — какой компонент за что
    отвечает.

- `services/pvlPostgrestApi.js` + `services/dataService.js` — методы
  по homeworks / progress / cohorts / mentors. Перечислить функции
  (имя + краткое описание + endpoint).

- `utils/pvlHomeworkAnswerRichText.js` — реализация. Как реально
  извлекается plain text из rich-text payload? Учитывает ли DOMPurify
  KEEP_CONTENT-gotcha (см. lesson 2026-05-04)?

### 2. AdminPanel.jsx

Какие сейчас разделы/табы? Структура. Есть ли таб с агрегатной
аналитикой по студентам?

### 3. Существующие PVL-screens в учительской

Что внутри:
- **Дашборд** — что показывает сейчас?
- **Результаты** — что показывает сейчас?
- **Очередь проверок** — логика выборки ДЗ?

### 4. Reusable-компоненты в Garden

- Есть ли утилита для CSV-download?
- Есть ли sortable-table component?
- Есть ли filter-bar / search-bar component?
- Есть ли модалка-экспорт / progress-indicator?

### 5. Routing

Любая навигация / route, которая может понадобиться для новой страницы
дашборда / выгрузки. Как сейчас устроен SPA-роутинг в Garden (если
есть)?

## Что вернуть

Структурированный отчёт **в файле**:

```
garden/docs/_session/2026-05-07_02_codeexec_recon_feat016_017_report.md
```

Структура:
- Section per задача, sub-section per файл/компонент.
- Цитаты кода с номерами строк.
- В конце — open questions от executor стороны, если что-то непонятно
  по бизнес-логике.

**НЕ apply, НЕ commit.** Стратег объединит твой code-side recon с
DB-side recon (своим) в общий отчёт.
