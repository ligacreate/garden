# Diff на ревью — копирайт ошибки логина (убираем ложное «подтвердите почту»)

**Дата:** 2026-07-16
**Повод:** Одета при неверном пароле увидела «…либо ваша почта не подтверждена…
найдите письмо подтверждения». В garden-auth **нет** подтверждения почты
(register сразу `status='active'`, grep по server.js — ноль confirm-путей).
Сообщение уводит человека искать несуществующее письмо.

## Что отдаёт бэкенд на логине (garden-auth /auth/login)
- **401 «Invalid credentials»** — и на неизвестный email, и на неверный пароль.
- **403 «Account suspended»** — если `users_auth.status != 'active'`.
- Ветки «Email not confirmed» НЕ существует (наследие Supabase-текстов).

## Изменение — App.jsx (handleLogin, ~строка 304)
```diff
-  msg = "Неверные данные, либо ваша почта не подтверждена. Проверьте пароль, найдите письмо подтверждения или попробуйте 'Создать аккаунт'.";
-} else if (msg.includes("Email not confirmed")) {   // мёртвая ветка
-  msg = "Ваша почта не подтверждена. Пожалуйста, найдите письмо…";
+  msg = "Неверный email или пароль. Проверьте раскладку клавиатуры и регистр букв. Если не помните пароль — нажмите «Забыли пароль?» ниже.";
+} else if (normalizedMsg.includes("account suspended")) {
+  msg = "Доступ к аккаунту приостановлен. Напишите нам — поможем восстановить доступ.";
```
- 401 → честный текст про email/пароль + указатель на «Забыли пароль?».
- Мёртвую ветку «Email not confirmed» заменил на реальный 403 «Account
  suspended» (был бы сырой английский). Fallback для прочего — как был.

## Проверки
- `npm run build` — зелёный.
- Дальше: commit СТРОГО App.jsx + этот _session, push → CI/FTP (App.jsx —
  source, не в paths-ignore → деплой сработает, окно 403). dist не коммитим.

## Ждёт 🟢 (это правка её текста — твоё слово по формулировке)
