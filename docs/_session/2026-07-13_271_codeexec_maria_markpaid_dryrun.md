# DRYRUN-ON-REVIEW — Мария Бардина: adminMarkPaid → 2026-08-17 (вернуть потерянные дни)

**Дата:** 2026-07-13 · **Автор:** codeexec · **Статус:** DRYRUN, НЕ применено, жду 🟢
**Recon:** READ-ONLY psql (prod), по запросу Ольги. **Связано:** фикс `2026-07-13_270_*payform_stacking_diff.md`

---

## Кто это (идентифицировано по recon, подтвердите user_id)

| поле | значение |
|---|---|
| name | **Мария Бардина** |
| user_id | `0b2c96cc-9b2a-496a-b5b9-0c7ef87b151f` |
| email | mb1@bk.ru · phone +79039168679 · tg_user_id 459549984 |
| role | leader · access_status **active** · subscription_status active |

> ⚠️ «Мария Романова» из заблокированного файла `..maria_romanova.md` — **другая** Мария (сброс пароля,
> не биллинг). Наша — Бардина: совпало 06.08 точно + событие payment_success + payform-путь.

## Подтверждение «видит 06.08» ✅

- `profiles.paid_until = 2026-08-06 23:59:59+03`.
- Кабинет (`ProfileView`): `new Date(paid_until).toLocaleDateString('ru-RU')` — **браузерная** TZ.
- Prod DB session TZ = **Europe/Moscow** (проверено `show timezone`). Для MSK-браузера рендерит **06.08.2026**. ✅

**«Расхождение на день из её сообщения» — гипотеза:** значение = 06.08 **23:59:59** MSK. Если устройство
Марии восточнее MSK (UTC+4/+5), `toLocaleDateString` сдвигает 06.08 23:59 → **07.08**. Тогда она пишет «07.08»,
а мы (MSK) видим 06.08 — ровно 1 день. Это косметика рендера (граница суток), не влияет на доступ.
После markPaid дата станет 17.08 23:59:59 — тот же эффект границы возможен для не-MSK устройств.

## Почему она недобрала дни (реконструкция, 3 payform-оплаты)

Все три — payform-товар без `paid_until` в пейлоаде → старый код перезаписывал `now()+31`, обнуляя остаток:

| # | payform | было (paid_until) | перезапись дала | стопка дала бы |
|---|---|---|---|---|
| 1 | 2026-05-18 08:56 | — | ~06-18 | 2026-06-18 |
| 2 | 2026-06-16 18:44 (за 2 дн до конца) | ~06-18 | ~07-16 (−2 дн) | 2026-07-18 |
| 3 | 2026-07-06 11:05 (за 10 дн до конца) | ~07-16 | **2026-08-06** (−10 дн) | **2026-08-18** |

Итого корректный paid_until при стопке ≈ **18.08** (make_interval, время от 1-й оплаты ~08:56). Сейчас 06.08.

> ⚠️ **Расхождение с целью стратега:** стратег указал **17.08** («вернуть ~11 дней»), мой recon даёт
> **~18.08** (~12 дней). Разница в 1 день = арифметика месяцев/от какой базы считать. **17.08 — консервативно
> (в пользу компании на 1 день).** Прошу подтвердить: ставим 17.08 (как сказал стратег) или 18.08 (точная стопка)?

## Dryrun: `POST /api/billing/admin/mark-paid`

```json
{ "user_id": "0b2c96cc-9b2a-496a-b5b9-0c7ef87b151f",
  "until_date": "2026-08-17",
  "note": "compensation: payform overwrite bug lost ~11-12 days (payments 05-18/06-16/07-06)",
  "idempotency_key": "manual-bardina-2026-08-17-fix270" }
```

Валидация проходит: формат `YYYY-MM-DD` ok · не в прошлом ok · idempotency_key задан.

**Эффект (applyPayment, until-режим):**
- `profiles.paid_until` → `2026-08-17 23:59:59+03` (`until::date + 1day − 1sec`).
- `access_status` → active (у неё уже active; не тронет `paused_manual`), `subscription_status=active`.
- `subscriptions.paid_until` синхронизируется на то же значение (чинит текущий рассинхрон 23:59:59 vs 11:05:06).
- Аудит-строка `payment_orders(provider='manual', status='paid', granted_until=17.08 23:59:59)`.
- **Кабинет после:** «Оплачено до **17.08.2026**». Нетто для Марии: **06.08 → 17.08 = +11 дней**.

## Перед apply — 2 подтверждения от стратега/Ольги
1. user_id `0b2c96cc…b151f` (Бардина) — верный человек?
2. Цель **17.08** (стратег) или **18.08** (точная стопка по recon)?

**НЕ применяю до 🟢.**
