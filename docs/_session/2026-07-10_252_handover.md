# HANDOVER — Фаза 3 (TG-доступ) + биллинг · состояние на 2026-07-10

Для нового codeexec-чата. Кратко, по делу.

## ✅ Задеплоено и живо на проде
- **mode=admit** (`TG_ACCESS_MODE=admit` в /opt/push-server/.env). TG-reconcile + join-poller работают.
  Поллер слушает `chat_join_request`, авто-approve оплаченных (матч по `telegram_user_id` ИЛИ `@username`,
  + бэкфилл uid из from.id). Kick — только planned, НЕ исполняется.
- **Миграция phase46** (`tg_access_actions`) применена (owner-only). Файл в git (`de26ec8`).
- **BILLING_SANDBOX=0** — боевой checkout. Реальные платежи через кабинет РАБОТАЮТ (Габрух 2000₽/1m, Старостина 5500₽/3m). `.env` забэкапан (`.env.bak-*`).
- Бот `@ligagardenbot` — админ в канале (`-1002377682177`) + чате (`-1002432957741`): `can_restrict_members` + `can_invite_users`. Токен в `.env` (`TG_ACCESS_BOT_TOKEN`), **НЕ ревокнут** (работаем на нём).
- Код TG-доступа (client/const/actions/reconcile/poller + server.mjs wiring) — задеплоен (`8dc3f2d`, `30e4e3f`).

## ⚠️ НЕ задеплоено — ВАЖНО (правка ошибочного «задеплоен» в _session/251)
- **Корневой фикс товаро-гейта вебхука (`isLigaProduct`) — НЕ на проде.** Код закоммичен (`6111818`, approved
  _session/250, тесты 22/22), но НЕ rsync'нут/не рестартнут. **Прод-вебхук ВСЁ ЕЩЁ товаро-слепой** → новый
  не-Лига recurring-платёж по-прежнему может выдать Лига-`paid_until` (класс Старостиной).
  → **Первый шаг нового чата: задеплоить** (`rsync billingLogic.mjs server.mjs` + restart) **или откатить.**
  Текущих ложных грантов 0 (scan _session/248); Старостина уже разово откачена.

## ⏳ Planned/gated (ждёт вечер)
- **KICK по Шиловой** (leader, `paused_expired`, реально в канале+чате): в `tg_access_actions` `status=planned`,
  НЕ исполнен. Пойдёт только при `mode=live`+`AUTOKICK` или через `POST /api/tg-access/confirm-kicks`.
  Порядок cutover без двойного кика: (1) Оля выключает TH-kick → (2) показать финальный KICK-список →
  (3) `mode=live` + confirm первого батча → (4) `AUTOKICK=1`. Габрух ушла из KICK сама (оплатила).

## Решённая модель — «кабинет-первый»
- Enforcement по `paid_until`; доступ в TG рулит НАШ бот (не TargetHunter; cutover TH позже).
- `paid_until` выдаётся только за Лига-товар (после деплоя фикса выше).
- Хард-правила бота: кик только известных истёкших (профиль+uid+`paid_until<now`+не exempt/manual);
  **незнакомца без профиля НЕ трогаем**; TOCTOU-перепроверка живого paid_until/членства перед кик.
- ADMIT: поллер авто-approve заявок оплаченных + персональные инвайты точечно.

## 🔭 Открытые треки
1. **Дизайн-док модели доставки join-ссылок** (recon [_session/251]) → потом стройка. 2 канала:
   кнопки в «Моя подписка» (on-platform, гейт `subActive=paid_until>=now`) + Prodamus «Платный контент»
   per-Лига-товар (off-platform, email; per-товар, с нашими ad-hoc products НЕ работает → нужен каталожный товар/SMTP).
2. **Кнопки «Вступить в канал/чат»** в «Моя подписка» ([ProfileView.jsx:614]) — фронт-diff в окно 403.
   Ссылки: канал `https://t.me/+dVRWs_cl2VA3OTVi`, чат `https://t.me/+GH0sjSaUzOc2N2Zi`.
3. **Оля в кабинете Prodamus**: «Платный контент» = обе ссылки на Лига-каталог-товаре + Email обязателен.
4. **Off-platform без профиля**: поллер их заявку не одобрит (unknown) → нужен «профиль» или ручное одобрение.
5. **Старостина** → `intern`, оплачена до 2026-10-10, uid есть → встанет в ADMIT на след. reconcile
   (или подаст заявку → поллер одобрит). Инвайт можно выдать.
6. **cutover TargetHunter** — при переходе в `mode=live`.

## Незапушенное / на ревью
- Product-gate код — закоммичен `6111818`, **НЕ задеплоен** (см. ⚠ выше). Рабочее дерево чистое.

## Ключевые доки (docs/_session/)
239 дизайн live · 240 impl-diff кода · 242 миграция applied · 245 активация admit ·
248 scan ложных грантов · 249+250 фикс вебхука (recon+impl) · 251 recon доставки ссылок.

## Инфра
- push-server: `/opt/push-server/`, systemd `push-server.service`; rsync `--exclude=.env --exclude='*.test.mjs'`; restart.
- Прод-БД: коннект как `gen_user` (владелец) через `DATABASE_URL`. **Правила: diff-on-review перед записями, dry→commit, self-guard.**
- Эндпоинты TG-доступа (requireAdmin): `POST /api/tg-access/run?mode=`, `GET /planned`, `POST /confirm-kicks`.
